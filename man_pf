.TH man_pf 1 "27 Feb 2018" "1.0"
.SH NAME
.B _printf - Formatted output conversion

.SH SYNOPSIS
#include "holberton.h"

 _printf [FORMAT STRING] [ARGUMENT]... 

.SH DESCRIPTION
Writes to the standard output according to the format passed through \
a parameter.

Additional arguments follow after the format string if integers, strings, or characters are to be printed, and the format string must contain a format specification.

%c - Specifier that takes a following char argument.

%s - Specifier that takes a following string argument.

%d - Specifier that takes a following integer. Any integer passed will be read as a signed decimal integer.

%i - Specifier that takes a following integer. The integer will be read as a decimal, hexadecimal, or octal type.

%% - Specifier that will be read as a percent sign.

.SH RETURN VALUE
Upon successful return, the function returns the number of characters printed (without the terminating null byte at the end of a string). 

If an output error occurs, returns a negative value.

.SH AUTHORS 
Collaboration by Becky Chen and Brian Chew